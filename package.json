{
  "name": "tvbox-auto-template",
  "version": "1.0.0",
  "description": "Auto check TVBox config sources and publish the fastest available config for TVBox App.",
  "type": "module",
  "private": true,
  "scripts": {
    "check": "node scripts/check-sources.js",
    "build": "node scripts/build-tvbox.js",
    "start": "node scripts/local-server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@fastify/static": "^8.1.1",
    "fastify": "^5.2.1"
  },
  "devDependencies": {}
}
